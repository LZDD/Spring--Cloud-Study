server:
  port: 8012
spring:
  application:
    name: service-consumer-movie-feign-customizing
eureka:
  client:
    service-url:
      defaultZone: http://admin:password123@localhost:8761/eureka/
    #register-with-eureka: true
    #fetch-registry: false
  instance:
    prefer-ip-address: true

logging:
  level:
    com.hand.FeignInter.UserFeignClient: DEBUG